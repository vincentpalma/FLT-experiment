#!/usr/bin/env bash

# This repository currently does not publish API docs.
# Keep this script as an intentional no-op so CI/jobs that invoke it still pass.
set -euo pipefail
echo "Skipping API docs build (intentionally disabled for this repository)."
